<template>
    <div class="checkout-page min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 py-8">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <!-- Header -->
            <div class="text-center mb-8">
                <h1 class="text-4xl font-bold text-gray-900 mb-2">Finalizar Compra</h1>
                <p class="text-lg text-gray-600">Complete seu pedido de forma segura e rápida</p>
            </div>

            <!-- Progress Steps -->
            <div class="mb-8">
                <div class="flex items-center justify-center space-x-4">
                    <div
                        v-for="(stepItem, index) in steps"
                        :key="index"
                        class="flex items-center"
                    >
                        <div
                            :class="[
                                'w-12 h-12 rounded-full flex items-center justify-center text-sm font-bold transition-all duration-300',
                                step > index + 1
                                    ? 'bg-green-500 text-white'
                                    : step === index + 1
                                      ? 'bg-blue-600 text-white shadow-lg'
                                      : 'bg-gray-300 text-gray-600'
                            ]"
                        >
                            <i v-if="step > index + 1" class="fas fa-check text-xs"></i>
                            <span v-else>{{ index + 1 }}</span>
                        </div>
                        <span
                            :class="[
                                'ml-3 text-sm font-medium transition-colors duration-300',
                                step >= index + 1 ? 'text-gray-900' : 'text-gray-500'
                            ]"
                        >
                            {{ stepItem.title }}
                        </span>
                        <div
                            v-if="index < steps.length - 1"
                            :class="[
                                'w-16 h-1 mx-4 rounded transition-colors duration-300',
                                step > index + 1 ? 'bg-green-500' : 'bg-gray-300'
                            ]"
                        ></div>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- Main Content -->
                <div class="lg:col-span-2">
                    <Transition name="step" mode="out-in">
                        <!-- Step 1: Endereço de Entrega -->
                        <div v-if="step === 1" key="step1" class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100">
                            <div class="flex items-center mb-6">
                                <i class="fas fa-map-marker-alt text-2xl text-blue-600 mr-3"></i>
                                <h2 class="text-2xl font-bold text-gray-900">Endereço de Entrega</h2>
                            </div>

                            <form @submit.prevent="validateAndNext(1)" class="space-y-6">
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div class="md:col-span-2">
                                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                                            Nome Completo *
                                        </label>
                                        <input
                                            v-model="address.name"
                                            required
                                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                            type="text"
                                            placeholder="Digite seu nome completo"
                                        />
                                    </div>

                                    <div class="md:col-span-2">
                                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                                            Endereço *
                                        </label>
                                        <input
                                            v-model="address.street"
                                            required
                                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                            type="text"
                                            placeholder="Rua, número, complemento"
                                        />
                                    </div>

                                    <div>
                                        <label class="block text-sm font-semibold text-gray-700 mb-2">
                                            Cidade *
                                        </label>
                                        <input
                                            v-model="address.city"
                                            required
                                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                            type="text"
                                            placeholder="Sua cidade"
                                        />
                                    </div>

                                    <div class="flex space-x-3">
                                        <div class="flex-1">
                                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                Estado *
                                            </label>
                                            <select
                                                v-model="address.state"
                                                required
                                                class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                            >
                                                <option value="">Selecione</option>
                                                <option value="AC">Acre</option>
                                                <option value="AL">Alagoas</option>
                                                <option value="AP">Amapá</option>
                                                <option value="AM">Amazonas</option>
                                                <option value="BA">Bahia</option>
                                                <option value="CE">Ceará</option>
                                                <option value="DF">Distrito Federal</option>
                                                <option value="ES">Espírito Santo</option>
                                                <option value="GO">Goiás</option>
                                                <option value="MA">Maranhão</option>
                                                <option value="MT">Mato Grosso</option>
                                                <option value="MS">Mato Grosso do Sul</option>
                                                <option value="MG">Minas Gerais</option>
                                                <option value="PA">Pará</option>
                                                <option value="PB">Paraíba</option>
                                                <option value="PR">Paraná</option>
                                                <option value="PE">Pernambuco</option>
                                                <option value="PI">Piauí</option>
                                                <option value="RJ">Rio de Janeiro</option>
                                                <option value="RN">Rio Grande do Norte</option>
                                                <option value="RS">Rio Grande do Sul</option>
                                                <option value="RO">Rondônia</option>
                                                <option value="RR">Roraima</option>
                                                <option value="SC">Santa Catarina</option>
                                                <option value="SP">São Paulo</option>
                                                <option value="SE">Sergipe</option>
                                                <option value="TO">Tocantins</option>
                                            </select>
                                        </div>

                                        <div class="w-32">
                                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                CEP *
                                            </label>
                                            <input
                                                v-model="address.zip"
                                                required
                                                class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                                type="text"
                                                placeholder="00000-000"
                                                maxlength="9"
                                                @input="formatZip"
                                            />
                                        </div>
                                    </div>
                                </div>

                                <div class="flex justify-end pt-6">
                                    <button
                                        type="submit"
                                        class="bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-8 py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105"
                                    >
                                        Continuar para Frete
                                        <i class="fas fa-arrow-right ml-2"></i>
                                    </button>
                                </div>
                            </form>
                        </div>

                        <!-- Step 2: Opções de Frete -->
                        <div v-if="step === 2" key="step2" class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100">
                            <div class="flex items-center mb-6">
                                <i class="fas fa-truck text-2xl text-green-600 mr-3"></i>
                                <h2 class="text-2xl font-bold text-gray-900">Opções de Frete</h2>
                            </div>

                            <form @submit.prevent="validateAndNext(2)" class="space-y-6">
                                <div class="space-y-4">
                                    <div
                                        v-for="option in shippingOptions"
                                        :key="option.id"
                                        @click="shipping = option.id"
                                        :class="[
                                            'border-2 rounded-xl p-6 cursor-pointer transition-all duration-300 hover:shadow-lg',
                                            shipping === option.id
                                                ? 'border-blue-500 bg-blue-50 shadow-lg'
                                                : 'border-gray-200 hover:border-gray-300'
                                        ]"
                                    >
                                        <div class="flex items-center justify-between">
                                            <div class="flex items-center space-x-4">
                                                <div :class="[
                                                    'w-4 h-4 rounded-full border-2 flex items-center justify-center',
                                                    shipping === option.id ? 'border-blue-500' : 'border-gray-300'
                                                ]">
                                                    <div v-if="shipping === option.id" class="w-2 h-2 bg-blue-500 rounded-full"></div>
                                                </div>
                                                <div>
                                                    <h3 class="font-bold text-gray-900">{{ option.name }}</h3>
                                                    <p class="text-sm text-gray-600">{{ option.description }}</p>
                                                </div>
                                            </div>
                                            <div class="text-right">
                                                <div class="font-bold text-lg text-gray-900">R$ {{ option.price.toFixed(2) }}</div>
                                                <div class="text-sm text-gray-500">{{ option.estimatedDays }}</div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="flex justify-between pt-6">
                                    <button
                                        type="button"
                                        @click="prevStep"
                                        class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-3 rounded-xl font-semibold transition-all duration-300"
                                    >
                                        <i class="fas fa-arrow-left mr-2"></i>
                                        Voltar
                                    </button>
                                    <button
                                        type="submit"
                                        class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white px-8 py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105"
                                    >
                                        Continuar para Pagamento
                                        <i class="fas fa-arrow-right ml-2"></i>
                                    </button>
                                </div>
                            </form>
                        </div>
                        </div>

                        <!-- Step 3: Pagamento -->
                        <div v-if="step === 3" key="step3" class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100">
                            <div class="flex items-center mb-6">
                                <i class="fas fa-credit-card text-2xl text-purple-600 mr-3"></i>
                                <h2 class="text-2xl font-bold text-gray-900">Forma de Pagamento</h2>
                            </div>

                            <form @submit.prevent="validateAndNext(3)" class="space-y-6">
                                <div class="space-y-4">
                                    <!-- Pix -->
                                    <div
                                        @click="payment = 'pix'"
                                        :class="[
                                            'border-2 rounded-xl p-6 cursor-pointer transition-all duration-300 hover:shadow-lg',
                                            payment === 'pix'
                                                ? 'border-blue-500 bg-blue-50 shadow-lg'
                                                : 'border-gray-200 hover:border-gray-300'
                                        ]"
                                    >
                                        <div class="flex items-center space-x-4">
                                            <div :class="[
                                                'w-4 h-4 rounded-full border-2 flex items-center justify-center',
                                                payment === 'pix' ? 'border-blue-500' : 'border-gray-300'
                                            ]">
                                                <div v-if="payment === 'pix'" class="w-2 h-2 bg-blue-500 rounded-full"></div>
                                            </div>
                                            <i class="fas fa-qrcode text-2xl text-green-600"></i>
                                            <div>
                                                <h3 class="font-bold text-gray-900">Pix</h3>
                                                <p class="text-sm text-gray-600">Pagamento instantâneo e sem taxas</p>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Boleto -->
                                    <div
                                        @click="payment = 'boleto'"
                                        :class="[
                                            'border-2 rounded-xl p-6 cursor-pointer transition-all duration-300 hover:shadow-lg',
                                            payment === 'boleto'
                                                ? 'border-blue-500 bg-blue-50 shadow-lg'
                                                : 'border-gray-200 hover:border-gray-300'
                                        ]"
                                    >
                                        <div class="flex items-center space-x-4">
                                            <div :class="[
                                                'w-4 h-4 rounded-full border-2 flex items-center justify-center',
                                                payment === 'boleto' ? 'border-blue-500' : 'border-gray-300'
                                            ]">
                                                <div v-if="payment === 'boleto'" class="w-2 h-2 bg-blue-500 rounded-full"></div>
                                            </div>
                                            <i class="fas fa-barcode text-2xl text-orange-600"></i>
                                            <div>
                                                <h3 class="font-bold text-gray-900">Boleto Bancário</h3>
                                                <p class="text-sm text-gray-600">Pagamento em até 3 dias úteis</p>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Cartão de Crédito -->
                                    <div
                                        @click="payment = 'cartao'"
                                        :class="[
                                            'border-2 rounded-xl p-6 cursor-pointer transition-all duration-300 hover:shadow-lg',
                                            payment === 'cartao'
                                                ? 'border-blue-500 bg-blue-50 shadow-lg'
                                                : 'border-gray-200 hover:border-gray-300'
                                        ]"
                                    >
                                        <div class="flex items-center space-x-4">
                                            <div :class="[
                                                'w-4 h-4 rounded-full border-2 flex items-center justify-center',
                                                payment === 'cartao' ? 'border-blue-500' : 'border-gray-300'
                                            ]">
                                                <div v-if="payment === 'cartao'" class="w-2 h-2 bg-blue-500 rounded-full"></div>
                                            </div>
                                            <i class="fas fa-credit-card text-2xl text-blue-600"></i>
                                            <div>
                                                <h3 class="font-bold text-gray-900">Cartão de Crédito</h3>
                                                <p class="text-sm text-gray-600">Parcele em até 12x sem juros</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Campos do Cartão -->
                                <Transition name="card-fields">
                                    <div v-if="payment === 'cartao'" class="space-y-4 bg-gray-50 rounded-xl p-6">
                                        <h4 class="font-semibold text-gray-900 mb-4">Dados do Cartão</h4>

                                        <div>
                                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                Número do Cartão *
                                            </label>
                                            <input
                                                v-model="card.number"
                                                required
                                                class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                                type="text"
                                                placeholder="0000 0000 0000 0000"
                                                maxlength="19"
                                                @input="formatCardNumber"
                                            />
                                        </div>

                                        <div class="grid grid-cols-2 gap-4">
                                            <div>
                                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                    Validade *
                                                </label>
                                                <input
                                                    v-model="card.expiry"
                                                    required
                                                    class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                                    type="text"
                                                    placeholder="MM/AA"
                                                    maxlength="5"
                                                    @input="formatExpiry"
                                                />
                                            </div>

                                            <div>
                                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                    CVV *
                                                </label>
                                                <input
                                                    v-model="card.cvv"
                                                    required
                                                    class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                                    type="text"
                                                    placeholder="123"
                                                    maxlength="4"
                                                />
                                            </div>
                                        </div>

                                        <div>
                                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                                Nome no Cartão *
                                            </label>
                                            <input
                                                v-model="card.name"
                                                required
                                                class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-200"
                                                type="text"
                                                placeholder="Nome como no cartão"
                                            />
                                        </div>
                                    </div>
                                </Transition>

                                <div class="flex justify-between pt-6">
                                    <button
                                        type="button"
                                        @click="prevStep"
                                        class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-3 rounded-xl font-semibold transition-all duration-300"
                                    >
                                        <i class="fas fa-arrow-left mr-2"></i>
                                        Voltar
                                    </button>
                                    <button
                                        type="submit"
                                        class="bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white px-8 py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105"
                                    >
                                        Revisar Pedido
                                        <i class="fas fa-arrow-right ml-2"></i>
                                    </button>
                                </div>
                            </form>
                        </div>
                        </div>

                        <!-- Step 4: Resumo do Pedido -->
                        <div v-if="step === 4" key="step4" class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100">
                            <div class="flex items-center mb-6">
                                <i class="fas fa-clipboard-check text-2xl text-indigo-600 mr-3"></i>
                                <h2 class="text-2xl font-bold text-gray-900">Revisar Pedido</h2>
                            </div>

                            <div class="space-y-6">
                                <!-- Resumo do Endereço -->
                                <div class="bg-gray-50 rounded-xl p-6">
                                    <h3 class="font-bold text-gray-900 mb-3 flex items-center">
                                        <i class="fas fa-map-marker-alt text-blue-600 mr-2"></i>
                                        Endereço de Entrega
                                    </h3>
                                    <div class="text-gray-700">
                                        <p class="font-semibold">{{ address.name }}</p>
                                        <p>{{ address.street }}</p>
                                        <p>{{ address.city }} - {{ address.state }}, {{ address.zip }}</p>
                                    </div>
                                </div>

                                <!-- Resumo do Frete -->
                                <div class="bg-gray-50 rounded-xl p-6">
                                    <h3 class="font-bold text-gray-900 mb-3 flex items-center">
                                        <i class="fas fa-truck text-green-600 mr-2"></i>
                                        Frete
                                    </h3>
                                    <div class="text-gray-700">
                                        <p v-if="selectedShippingOption" class="font-semibold">
                                            {{ selectedShippingOption.name }}
                                        </p>
                                        <p class="text-sm">{{ selectedShippingOption?.description }}</p>
                                        <p class="text-sm text-gray-600">{{ selectedShippingOption?.estimatedDays }}</p>
                                    </div>
                                </div>

                                <!-- Resumo do Pagamento -->
                                <div class="bg-gray-50 rounded-xl p-6">
                                    <h3 class="font-bold text-gray-900 mb-3 flex items-center">
                                        <i class="fas fa-credit-card text-purple-600 mr-2"></i>
                                        Forma de Pagamento
                                    </h3>
                                    <div class="text-gray-700">
                                        <p class="font-semibold">
                                            <span v-if="payment === 'pix'">Pix</span>
                                            <span v-else-if="payment === 'boleto'">Boleto Bancário</span>
                                            <span v-else-if="payment === 'cartao'">Cartão de Crédito</span>
                                        </p>
                                        <p v-if="payment === 'cartao'" class="text-sm mt-2">
                                            **** **** **** {{ card.number.slice(-4) }}
                                        </p>
                                    </div>
                                </div>

                                <!-- Resumo dos Produtos -->
                                <div class="bg-gray-50 rounded-xl p-6">
                                    <h3 class="font-bold text-gray-900 mb-3 flex items-center">
                                        <i class="fas fa-shopping-bag text-orange-600 mr-2"></i>
                                        Produtos ({{ cart.length }})
                                    </h3>
                                    <div class="space-y-3">
                                        <div
                                            v-for="item in cart"
                                            :key="item.id"
                                            class="flex items-center justify-between py-2 border-b border-gray-200 last:border-b-0"
                                        >
                                            <div class="flex items-center space-x-3">
                                                <div class="w-12 h-12 bg-gradient-to-br from-blue-100 to-purple-100 rounded-lg flex items-center justify-center">
                                                    <i class="fas fa-flip-flops text-blue-600"></i>
                                                </div>
                                                <div>
                                                    <p class="font-semibold text-gray-900">{{ item.name }}</p>
                                                    <p class="text-sm text-gray-600">Quantidade: {{ item.qty }}</p>
                                                </div>
                                            </div>
                                            <div class="text-right">
                                                <p class="font-bold text-gray-900">R$ {{ (item.price * item.qty).toFixed(2) }}</p>
                                                <p class="text-sm text-gray-600">R$ {{ item.price }} cada</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Total -->
                                <div class="bg-gradient-to-r from-blue-600 to-purple-600 rounded-xl p-6 text-white">
                                    <div class="flex justify-between items-center text-xl font-bold">
                                        <span>Total do Pedido</span>
                                        <span>R$ {{ total.toFixed(2) }}</span>
                                    </div>
                                </div>

                                <div class="flex justify-between pt-6">
                                    <button
                                        type="button"
                                        @click="prevStep"
                                        class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-3 rounded-xl font-semibold transition-all duration-300"
                                    >
                                        <i class="fas fa-arrow-left mr-2"></i>
                                        Voltar
                                    </button>
                                    <button
                                        @click="submitOrder"
                                        :disabled="isSubmitting"
                                        class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 disabled:from-gray-400 disabled:to-gray-500 text-white px-8 py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105 disabled:transform-none disabled:cursor-not-allowed"
                                    >
                                        <i v-if="isSubmitting" class="fas fa-spinner fa-spin mr-2"></i>
                                        {{ isSubmitting ? 'Processando...' : 'Finalizar Compra' }}
                                        <i v-if="!isSubmitting" class="fas fa-check ml-2"></i>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Step 5: Confirmação -->
                        <div v-if="step === 5" key="step5" class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100 text-center">
                            <div class="mb-6">
                                <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                    <i class="fas fa-check-circle text-4xl text-green-600"></i>
                                </div>
                                <h2 class="text-3xl font-bold text-gray-900 mb-2">Pedido Realizado!</h2>
                                <p class="text-lg text-gray-600">Seu pedido foi processado com sucesso.</p>
                            </div>

                            <div class="bg-gray-50 rounded-xl p-6 mb-6">
                                <h3 class="font-bold text-gray-900 mb-4">Detalhes do Pedido</h3>
                                <div class="space-y-2 text-left">
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">Número do pedido:</span>
                                        <span class="font-semibold">#{{ orderNumber }}</span>
                                    </div>
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">Valor total:</span>
                                        <span class="font-semibold">R$ {{ total.toFixed(2) }}</span>
                                    </div>
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">Forma de pagamento:</span>
                                        <span class="font-semibold">
                                            <span v-if="payment === 'pix'">Pix</span>
                                            <span v-else-if="payment === 'boleto'">Boleto</span>
                                            <span v-else>Cartão</span>
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <div class="space-y-4">
                                <p class="text-gray-600">
                                    Você receberá um e-mail de confirmação em breve com todos os detalhes.
                                </p>

                                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                                    <router-link
                                        to="/"
                                        class="bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-8 py-3 rounded-xl font-bold transition-all duration-300 transform hover:scale-105 shadow-lg"
                                    >
                                        <i class="fas fa-home mr-2"></i>
                                        Voltar ao Início
                                    </router-link>
                                    <router-link
                                        to="/pedidos"
                                        class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-8 py-3 rounded-xl font-semibold transition-all duration-300"
                                    >
                                        <i class="fas fa-list mr-2"></i>
                                        Meus Pedidos
                                    </router-link>
                                </div>
                            </div>
                        </div>
                    </Transition>
                </div>
            </div>

<script setup>
import { ref, computed } from "vue";
import { useRouter } from "vue-router";

const step = ref(1);
const address = ref({ name: "", street: "", city: "", state: "", zip: "" });
const shipping = ref("");
const payment = ref("");
const card = ref({ number: "", expiry: "", cvv: "", name: "" });
const cart = ref([]);
const router = useRouter();

const CART_KEY = "ecommerce_cart";

const loadCart = () => {
    const saved = localStorage.getItem(CART_KEY);
    cart.value = saved ? JSON.parse(saved) : [];
};

const total = computed(() => {
    let subtotal = cart.value.reduce(
        (sum, item) => sum + item.price * item.qty,
        0,
    );
    let frete =
        shipping.value === "expresso"
            ? 40
            : shipping.value === "normal"
              ? 20
              : 0;
    return subtotal + frete;
});

const nextStep = () => {
    step.value++;
};
const prevStep = () => {
    step.value--;
};

const submitOrder = () => {
    // Aqui você integraria com a API de pedidos
    localStorage.removeItem(CART_KEY);
    step.value = 5;
};

loadCart();
</script>

<style scoped>
.input {
    width: 100%;
    padding: 0.5rem 0.75rem;
    border: 1px solid #d1d5db;
    border-radius: 0.375rem;
    outline: none;
    transition: box-shadow 0.2s;
}
.input:focus {
    box-shadow: 0 0 0 2px #2563eb33;
    border-color: #2563eb;
}
.btn {
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
    font-weight: 600;
    transition:
        background 0.2s,
        color 0.2s;
}
.btn-primary {
    background: #2563eb;
    color: #fff;
}
.btn-primary:hover {
    background: #1d4ed8;
}
.btn-outline {
    border: 1px solid #2563eb;
    color: #2563eb;
    background: #fff;
}
.btn-outline:hover {
    background: #f0f6ff;
}
</style>
